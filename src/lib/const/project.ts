import { PUBLIC_APP_VERSION } from '$env/static/public'
import { DEEPWIKI_URL } from './deepwiki'
import { GITHUB_URL } from './github'

export const APP_VERSION = PUBLIC_APP_VERSION

export const APP_REPO = `${GITHUB_URL}/rijuma/rigoli.dev`

export const APP_WIKI = `${DEEPWIKI_URL}/rijuma/rigoli.dev`
